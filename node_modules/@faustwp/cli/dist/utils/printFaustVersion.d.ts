/**
 * Sanitizes the version from a dependency in package.json.
 *
 * @param version The dependency version.
 * @returns A sanitized version or undefined if the version is a path.
 */
export declare function sanitizePackageJsonVersion(_version: string | undefined): string | undefined;
export declare function printFaustVersion(): void;
